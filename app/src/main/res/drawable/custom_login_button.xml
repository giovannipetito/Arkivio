<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- DISABLED -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient android:endColor="@color/grey_1" android:startColor="@color/grey_1" />
            <stroke android:width="2dp" android:color="@color/grey_1" />
        </shape>
    </item>

    <!-- PRESSED -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient android:endColor="@color/transparent" android:startColor="@color/transparent" />
            <stroke android:width="2dp" android:color="@color/grey_2" />
        </shape>
    </item>

    <!-- PRESSED -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient android:endColor="@color/transparent" android:startColor="@color/transparent" />
            <stroke android:width="2dp" android:color="@color/grey_2" />
        </shape>
    </item>

    <!-- ENABLED -->
    <item android:state_enabled="true" android:state_pressed="false">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient android:endColor="@color/dark" android:startColor="@color/dark" />
            <stroke android:width="2dp" android:color="@color/dark" />
        </shape>
    </item>

</selector>